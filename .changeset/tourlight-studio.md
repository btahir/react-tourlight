---
"react-tourlight": minor
---

Add a free, source-owned visual Studio with live target picking, local drafts, undo/redo, shared JSON import/export, current-page diagnostics, and real runtime preview. Introduce a validated TourDocument format, JSON Schema, CLI, portable agent skill, optional local MCP workspace, and checklist/guide-launcher components under separate imports.

Fix tour registration across rerenders, cancellable async transitions, target readiness and invalid selectors, interactive keyboard access, partial theme overrides, and persistence/resume edge cases. Include browser-tested authoring, runtime, and self-hosting examples.
